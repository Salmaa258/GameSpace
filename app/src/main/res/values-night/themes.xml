<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GameSpace" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/blue_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Button -->
        <item name="materialButtonStyle">@style/boton_noche</item>

        <!-- TextInputLayout -->
        <item name="textInputStyle">@style/TextInput_noche</item>

        <!--Spinner-->
        <item name="spinnerStyle">@style/Spinner_noche</item>

        <!--Divider
        <item name="dividerColor">@style/divider_noche</item>-->

    </style>
</resources>